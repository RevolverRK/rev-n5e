/**
 * Contains multiple existing Browser. Enum 0 (Unknown) is the default value.
 */
export enum BrowserType {
    Unknown = 0,
    Firefox = 1,
    Chrome = 2,
    Safari = 3,
    InternetExplorer = 4,
    Edge = 5,
    Opera = 6,
    Vivaldi = 7,
    Yandex = 8
}